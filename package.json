{
  "name": "stradit-landing",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "build:site": "next build && node scripts/trim-static-export.mjs && node scripts/verify-out.mjs",
    "build:compressed": "npm run build:site && cd out && zip -9 -r ../stradit-site.zip . && cd .. && ls -lh stradit-site.zip && du -sh out",
    "optimize:media:site": "node scripts/optimize-media.mjs --page-assets",
    "build:compressed:site": "npm run optimize:media:site && npm run build:compressed",
    "build:compressed:media": "npm run optimize:media && npm run build:compressed",
    "start": "next start",
    "optimize:media": "node scripts/optimize-media.mjs",
    "pack:zip": "npm run build:compressed"
  },
  "dependencies": {
    "next": "15.5.15",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "ffmpeg-static": "^5.3.0",
    "typescript": "^5"
  }
}
